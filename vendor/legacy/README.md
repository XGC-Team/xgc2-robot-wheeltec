# Vendor leftovers (not packaged)

Upstream navigation, camera, maps, costmap params, and extra URDFs.
Kept as source reference. Not in `ros.packages` / `apt.packages`.
Visual product model is `xgc2-mecanum-description`, not these files.
